# Sistema Contable

Sistema contable multi-tenant con .NET Core 8, Dapper y PostgreSQL.

## Requisitos
- .NET 8 SDK
- PostgreSQL 15+
- Docker (opcional)

## Instalacion
```bash
dotnet restore
docker-compose up -d
dotnet run --project src/SistemaContable.API
```
